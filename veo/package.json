{
  "name": "ui-test-runner",
  "version": "1.0.0",
  "description": "Automated UI Testing Environment — Express service with Puppeteer and Firestore session sync",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase": "^10.14.0",
    "firebase-admin": "^12.2.0",
    "puppeteer-core": "^22.10.0",
    "dotenv": "^16.4.5",
    "morgan": "^1.10.0",
    "helmet": "^7.1.0",
    "express-async-errors": "^3.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.3",
    "eslint": "^9.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
