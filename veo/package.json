{
  "name": "puppeteer-tls-proxy",
  "version": "1.0.0",
  "description": "Node.js proxy service using real Chrome (BoringSSL) to bypass Google TLS fingerprinting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "puppeteer": "^21.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
